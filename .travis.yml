# Configuration script to Travis-CI
language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"
install:
  - pip install pipenv coveralls[yaml]
  - pipenv install --dev
script:
  - pytest --cov src/ofxstatement
after_success:
  coveralls
